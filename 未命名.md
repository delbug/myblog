项目已在本地完整跑起来。

## 当前状态

| 项目             | 状态                                   |
| ---------------- | -------------------------------------- |
| MySQL            | 已启动（Homebrew，root 密码 `123456`） |
| 数据库 `blog_db` | 表结构已推送                           |
| 种子数据         | 已写入（admin 账号 + 示例文章）        |
| 开发服务器       | 运行中 → **http://localhost:3000**     |

## 访问地址

- **前台：** http://localhost:3000  
- **示例文章：** http://localhost:3000/posts/welcome-to-my-blog  
- **后台：** http://localhost:3000/admin/login（`admin` / `admin123`）

## 我实际做了什么

1. 用 `root / 123456` 连接 MySQL（你提供的密码）
2. 创建应用账号 `blog / blog123` 并授权 `blog_db`
3. 执行 `npm run db:push` 建表
4. 执行 `npm run db:seed` 写入初始数据
5. 执行 `npm run dev` 启动开发服务
6. 用 curl 验证首页、API、文章页均返回 200

## 操作记录文档

每一步命令、目的、结果都写在：

**[`docs/LOCAL_SETUP_LOG.md`](docs/LOCAL_SETUP_LOG.md)**

以后自己启动只需：

```bash
brew services start mysql   # MySQL 未运行时
cd /Users/xiaojiang/web/demo-vuessr
npm run dev
```